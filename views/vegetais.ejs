<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vegetais</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f3f3f3;
            margin: 0;
        }
        h1 {
            text-align: center;
            color: #2c7a2c;
            margin: 20px 0;
        }
        .filtro-categoria {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }
        .filtro-categoria select {
            padding: 10px;
            font-size: 16px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        .produtos {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }
        .produto {
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 15px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .produto img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
        }
        .produto h3 {
            font-size: 18px;
            margin: 10px 0;
        }
        .produto p {
            color: #666;
            font-size: 14px;
            margin-bottom: 10px;
        }
        .produto .preco {
            font-size: 16px;
            color: #2c7a2c;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Produtos</h1>

    <!-- Filtro de categoria -->
    <div class="filtro-categoria">
        <form method="GET" action="/produtos">
            <select name="categoria" onchange="this.form.submit()">
                <option value="">Todas as Categorias</option>
                <option value="verdura" <%= categoriaSelecionada === 'verdura' ? 'selected' : '' %>>Verdura</option>
                <option value="legumes" <%= categoriaSelecionada === 'legumes' ? 'selected' : '' %>>Legumes</option>
                <option value="frutas" <%= categoriaSelecionada === 'frutas' ? 'selected' : '' %>>Frutas</option>
                <option value="kits" <%= categoriaSelecionada === 'kits' ? 'selected' : '' %>>Kits</option>
            </select>
        </form>
    </div>

    <!-- Exibição dos produtos -->
    <div class="produtos">
        <% produtos.forEach(produto => { %>
            <div class="produto">
                <img src="/uploads/<%= produto.imagem %>" alt="<%= produto.nome_produto %>">
                <h3><%= produto.nome_produto %></h3>
                <p><%= produto.descricao %></p>
                <p class="preco">R$ <%= parseFloat(produto.preco).toFixed(2) %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>
