<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Frutas</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <!-- Inclua o conteúdo do header aqui -->
    </header>
    <main>
        <h1>Frutas</h1>
        <div class="products">
            <% frutas.forEach(fruta => { %>
                <div class="product-card">
                    <img src="<%= fruta.imagem %>" alt="<%= fruta.nome %>" class="product-image">
                    <div class="product-description" style="display: none;">
                        <p><%= fruta.descricao %></p>
                    </div>
                </div>
            <% }); %>
        </div>
    </main>
    <footer>
        <!-- Inclua o conteúdo do footer aqui -->
    </footer>
</body>
</html>