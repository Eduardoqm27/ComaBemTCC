<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ComaBemTCC</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <header>
        <div class="header-container">
            <img src="/img/logoCB.png" alt="Imagem Logo" class="logo"/>
            <form action="/pesquisar" method="GET" class="search-form">
                <input type="text" name="query" placeholder="Pesquisar produtos..." class="search-input" />
                <button type="submit" class="search-btn"><i class="bi bi-search"></i></button>
            </form>
            <a href="/carrinho" class="cart-link">
                <i class="bi bi-cart4 cart-icon"></i>
            </a>
        </div>
    </header>

    <nav class="menu-lateral"> 
        <div class="btn-expandir">
            <i class="bi bi-list"></i>
        </div>

        <ul class="ul">
            <li class="item-menu">
                <a href="/" data-link="/views/home.ejs">
                    <span class="icon"><i class="bi bi-house"></i></span>
                    <span class="txt-link">Início</span>
                </a>
            </li>
            <li class="item-menu">
                <a href="/produto/categoria" data-link="/views/categoria/categoria.ejs">
                    <span class="icon"><i class="bi bi-columns-gap"></i></span>
                    <span class="txt-link">Categorias</span>
                </a>
            </li>
            <li class="item-menu">
                <a href="/perfil" data-link="/views/user/perfil.ejs">
                    <span class="icon"><i class="bi bi-person-bounding-box"></i></span>
                    <span class="txt-link">Usuário</span>
                </a>
            </li>            
            <li class="item-menu">
                <a href="/carrinho" data-link="/views/carrinho.ejs">
                    <span class="icon"><i class="bi bi-cart4"></i></span>
                    <span class="txt-link">Carrinho</span>
                </a>
            </li>
        </ul>
    </nav>

    <div class="content">
        <h1>Produtos em Promoção</h1>
        <div class="produtos-grid">
            <% if (produtos && produtos.length > 0) { %>
                <% produtos.forEach(produto => { %>
                    <div class="card-produto">
                        <img src="/img/<%= produto.imagem %>" alt="<%= produto.nome_produto %>">
                        <h4><%= produto.nome_produto %></h4>
                        <p>Preço: R$ <%= parseFloat(produto.preco).toFixed(2) %></p>
                        <a href="/produto/<%= produto.id_produto %>">Ver Mais</a>
                    </div>
                <% }); %>
            <% } else { %>
                <p>Nenhum produto em promoção disponível.</p>
            <% } %>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 ComaBemTCC</p>
    </footer>

    <script src="/public/js/scripts.js"></script>
</body>
</html>
