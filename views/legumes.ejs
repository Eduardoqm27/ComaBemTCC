<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Legumes</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <!-- Inclua o conteúdo do header aqui -->
    </header>
    <main>
        <h1>Legumes</h1>
        <div class="products">
            <% legumes.forEach(legume => { %>
                <div class="product-card">
                    <img src="<%= legume.imagem %>" alt="<%= legume.nome %>" class="product-image">
                    <div class="product-description" style="display: none;">
                        <p><%= legume.descricao %></p>
                    </div>
                </div>
            <% }); %>
        </div>
    </main>
    <footer>
        <!-- Inclua o conteúdo do footer aqui -->
    </footer>
</body>
</html>